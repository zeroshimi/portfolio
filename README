1, docker-compose.ymlのあるディレクトリで

    docker-compose up -d

2,その後 API側→docker exec -it FastAPI sh、Web側→docker-compose exec nginx bashでコンテナの内部に入る

3、API側の場合
    pip install -r requirements.txt
    uvicorn main:app --reload --host 0.0.0.0
    をしFastApi起動

4、Web側の場合
    npm install
    npm run devで起動

